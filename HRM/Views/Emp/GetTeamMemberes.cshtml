@model IEnumerable<DataAccessLayer.AspNetUser>

@{
    ViewBag.Title = "GetTeamMembers";
    Layout = "~/Views/Shared/_EmpLayout.cshtml";
}

<h2 class="text-center">My Team Info</h2>

<div class="container" style="margin-top:10px">

<div class="row">

    <div class="col-lg-12 col-lg-offset-0 alert alert-info">
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Email)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PhoneNumber)
                </th>

                <th>
                    @Html.DisplayNameFor(model => model.UserName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DepartmentId)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PhoneNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UserName)
                    </td>
                    <td>
                        @*@if (item.DepartmentId == 1)
                        {
                            ViewBag.dept = "Testing";

                        }
                        else if (item.Deptid == 2)
                        {
                            ViewBag.dept = "Testing";
                        }
                        else if (item.Deptid == 3)
                        {
                            ViewBag.dept = "Testing";
                        }*@
                        @Html.DisplayFor(modelItem => item.DepartmentId)

                    </td>
                </tr>
            }

        </table>
        </div>
    </div>
    </div>
